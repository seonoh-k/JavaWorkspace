
public class LambdaClass { // 2.10
	
	// 람다식
	// 함수를 간략하게 표현하는 방법
	// 이름 없는 함수. 이름 없는 함수는 직접 사용할 수 없다.
	// 독립된 객체로 사용할 수 있다
	// 람다식에 맞는 함수형 인터페이스를 작성한다.
	
//	int max(int x, int y) {
//		return x > y ? x : y;
//	}
//	
//	// 람다식
//	// 전부 같은 표현
//	(int x, int y) -> {
//		return x > y ? x : y;
//	}
//	
//	(int x, int y) -> x > y ? x : y; // return 생략
//	
//	(x, y) -> x > y ? x : y; // 변수 타입 생략
//	
//	x -> x * x; // 매개변수가 하나일 경우 매개변수 괄호 생략 가능
	
	
}
